# codex

A simple Python worm game using the `curses` module.

## Running the game

```bash
python worm_game.py
```

Use the arrow keys to move the worm. Eat the π symbols to grow and avoid
hitting the borders or yourself. Press `q` to quit.
